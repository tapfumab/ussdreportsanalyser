server:
  port: 8914

spring:
  application:
    name : ussdreportsanalyser
  datasource:
    url: jdbc:mysql://localhost:3306/ussd_reports_analyser?autoReconnect=true&createDatabaseIfNotExist=true&useSSL=false
    username: developer
    password: 'D3v3l@p3r'

  jpa:
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.MySQL8Dialect

  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2KB
      max-file-size: 200MB
      max-request-size: 215MB

secret:
  key: 4be86562bd6bd134a0bae78c41f3c3f50edc613831a4076e938058af7353e2f2

logging:
  file:
    name: /data/logs/${spring.application.name}.log

#Active Directory Configurations
active.directory:
  domain: 'netone.co.zw'
  url: 'ldap://10.95.10.63:389'
  root_dn: 'dc=netone,dc=co,dc=zw'




